<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sast.crs.mapper.FileMapper">
    <insert id="upsertFile" parameterType="com.sast.crs.entity.File">
        INSERT INTO file (com_id, user_code, input, url)
        VALUES (#{comId}, #{userCode}, #{input}, #{url})
        ON DUPLICATE KEY UPDATE
            url = VALUES(url)
    </insert>
</mapper>
